# Isolated host tools for the DJConnect Raspberry Pi Pico 2 W MicroPython path.
# Keep this list separate from product dependencies: it belongs in the shared
# developer-tool virtual environment, never in a Pico firmware image.
mpremote>=1.26,<2
micropython-stubber>=1.24,<2
ruff==0.16.0
