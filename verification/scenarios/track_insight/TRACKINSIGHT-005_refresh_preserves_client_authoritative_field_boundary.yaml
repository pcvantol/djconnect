id: TRACKINSIGHT-005
title: Refresh preserves client-authoritative field boundary
description: Verify that DJConnect platform behavior for refresh preserves client-authoritative field boundary matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for refresh preserves client-authoritative field boundary.
owner: DJConnect Verification
category: Track Insight
subcategory: track_insight
priority: P1
risk: medium
verification_level: V3
automation_level: ENVIRONMENT_DEPENDENT
required_components: [HA, Apple, Pi]
supported_platforms: [Home Assistant, iOS, Raspberry Pi]
required_locales: [representative]
required_build_types: [Debug, Instrumented]
required_capabilities: [profiles, request_context]
required_profiles: [personal_profile, household_profile]
required_backends: [Spotify Direct]
required_devices: [djconnect_client]
required_environment: [configured_ha_instance]
requires:
  capabilities:
    - apple.runtime
    - djconnect.capabilities
    - djconnect.loaded
    - djconnect.track_insight
    - evidence.storage
    - ha.access_token
    - ha.logs
    - ha.rest
    - ha.runtime
    - ha.services
    - ha.websocket
    - music.fake_backend
    - pi.runtime
  services:
    - fake_music_backend
    - homeassistant
  integrations:
    - djconnect
  bootstrap:
    - djconnect.loaded
    - ha.auth
  resources:
    network: local
    persistent_storage: false
  hardware:
    - raspberry_pi
  secrets:
    - ha.access_token
  persistence: ephemeral
preconditions:
  - The verification environment contains the components required for refresh preserves client-authoritative field boundary.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for refresh preserves client-authoritative field boundary.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for refresh preserves client-authoritative field boundary.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  backend:
    - Refresh preserves client-authoritative field boundary follows the accepted platform ownership and contract boundaries.
    - Structured errors are returned for unsupported or invalid state.
  client:
    - Clients render backend-owned state without reconstructing personal intelligence locally.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: refresh_preserves_client_authoritative_field_boundary
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - Refresh preserves client-authoritative field boundary behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 120
  execution_seconds: 120
  cleanup_seconds: 60
retry_policy:
  mode: CONTROLLED_RETRY
  max_attempts: 2
  reason: Runtime, hardware or live-service behavior may be transient.
  delay_seconds: 5
artifacts:
  - type: logs
    required: true
    description: Sanitized evidence for refresh preserves client-authoritative field boundary.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Internal, Requires redaction]
destructive: false
tags: [track_insight, track_insight]
estimated_duration: 5m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
