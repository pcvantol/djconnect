id: RELEASE-003
title: Firmware release manifest includes checksums
description: Verify that DJConnect platform behavior for firmware release manifest includes checksums matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for firmware release manifest includes checksums.
owner: DJConnect Verification
category: Release
subcategory: release
priority: P1
risk: medium
verification_level: V6
automation_level: FULL
required_components: [Release Repo]
supported_platforms: [Release Repo]
required_locales: [not_applicable]
required_build_types: [Release-equivalent, Production artifact]
required_capabilities: []
required_profiles: []
required_backends: []
required_devices: []
required_environment: [release_artifacts_available]
requires:
  capabilities:
    - evidence.storage
    - github.ci
    - release.artifacts
  resources:
    network: internet
    persistent_storage: false
  persistence: ephemeral
preconditions:
  - The verification environment contains the components required for firmware release manifest includes checksums.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for firmware release manifest includes checksums.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for firmware release manifest includes checksums.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  backend:
    - Firmware release manifest includes checksums follows the accepted platform ownership and contract boundaries.
    - Structured errors are returned for unsupported or invalid state.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: firmware_release_manifest_includes_checksums
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - Firmware release manifest includes checksums behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 120
  execution_seconds: 120
  cleanup_seconds: 60
retry_policy:
  mode: NONE
  max_attempts: 1
artifacts:
  - type: artifact_metadata
    required: true
    description: Sanitized evidence for firmware release manifest includes checksums.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Public]
destructive: false
tags: [release, release]
estimated_duration: 5m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
