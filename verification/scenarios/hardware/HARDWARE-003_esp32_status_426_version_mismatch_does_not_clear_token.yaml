id: HARDWARE-003
title: ESP32 status 426 version mismatch does not clear token
description: Verify that DJConnect platform behavior for esp32 status 426 version mismatch does not clear token matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for esp32 status 426 version mismatch does not clear token.
owner: DJConnect Verification
category: Hardware
subcategory: esp32
priority: P0
risk: high
verification_level: V5
automation_level: SEMI_AUTOMATED
required_components: [HA, ESP32]
supported_platforms: [Home Assistant, ESP32]
required_locales: [representative]
required_build_types: [Hardware, Release-equivalent]
required_capabilities: []
required_profiles: [personal_profile, household_profile]
required_backends: []
required_devices: [djconnect_esp32]
required_environment: [configured_ha_instance]
preconditions:
  - The verification environment contains the components required for esp32 status 426 version mismatch does not clear token.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for esp32 status 426 version mismatch does not clear token.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for esp32 status 426 version mismatch does not clear token.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  backend:
    - ESP32 status 426 version mismatch does not clear token follows the accepted platform ownership and contract boundaries.
    - Structured errors are returned for unsupported or invalid state.
  hardware:
    - Hardware behavior remains bounded to device/runtime responsibilities.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: esp32_status_426_version_mismatch_does_not_clear_token
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - ESP32 status 426 version mismatch does not clear token behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 300
  execution_seconds: 120
  cleanup_seconds: 60
retry_policy:
  mode: CONTROLLED_RETRY
  max_attempts: 2
  reason: Runtime, hardware or live-service behavior may be transient.
  delay_seconds: 5
artifacts:
  - type: serial_logs
    required: true
    description: Sanitized evidence for esp32 status 426 version mismatch does not clear token.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Internal, Requires redaction]
destructive: false
tags: [hardware, esp32]
estimated_duration: 10m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
