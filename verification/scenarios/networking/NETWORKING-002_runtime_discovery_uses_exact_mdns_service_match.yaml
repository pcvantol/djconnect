id: NETWORKING-002
title: Runtime discovery uses exact mdns service match
description: Verify that DJConnect platform behavior for runtime discovery uses exact mdns service match matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for runtime discovery uses exact mdns service match.
owner: DJConnect Verification
category: Networking
subcategory: networking
priority: P1
risk: medium
verification_level: V3
automation_level: ENVIRONMENT_DEPENDENT
required_components: [HA, ESP32]
supported_platforms: [Home Assistant, ESP32]
required_locales: [representative]
required_build_types: [Debug, Instrumented]
required_capabilities: []
required_profiles: [personal_profile, household_profile]
required_backends: []
required_devices: []
required_environment: [configured_ha_instance]
preconditions:
  - The verification environment contains the components required for runtime discovery uses exact mdns service match.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for runtime discovery uses exact mdns service match.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for runtime discovery uses exact mdns service match.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  backend:
    - Runtime discovery uses exact mdns service match follows the accepted platform ownership and contract boundaries.
    - Structured errors are returned for unsupported or invalid state.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: runtime_discovery_uses_exact_mdns_service_match
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - Runtime discovery uses exact mdns service match behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 120
  execution_seconds: 120
  cleanup_seconds: 60
retry_policy:
  mode: CONTROLLED_RETRY
  max_attempts: 2
  reason: Runtime, hardware or live-service behavior may be transient.
  delay_seconds: 5
artifacts:
  - type: logs
    required: true
    description: Sanitized evidence for runtime discovery uses exact mdns service match.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Internal, Requires redaction]
destructive: false
tags: [networking, networking]
estimated_duration: 5m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
