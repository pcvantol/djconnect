id: MUSICDNA-013
title: Private Session does not add listening knowledge
description: Verify that DJConnect platform behavior for private session does not add listening knowledge matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for private session does not add listening knowledge.
owner: DJConnect Verification
category: Music DNA
subcategory: music_dna
priority: P0
risk: high
verification_level: V3
automation_level: ENVIRONMENT_DEPENDENT
required_components: [HA]
supported_platforms: [Home Assistant]
required_locales: [representative]
required_build_types: [Debug, Instrumented]
required_capabilities: [profiles, request_context]
required_profiles: [personal_profile, household_profile]
required_backends: [Spotify Direct]
required_devices: []
required_environment: [configured_ha_instance]
preconditions:
  - The verification environment contains the components required for private session does not add listening knowledge.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for private session does not add listening knowledge.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for private session does not add listening knowledge.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  backend:
    - Private Session does not add listening knowledge follows the accepted platform ownership and contract boundaries.
    - Structured errors are returned for unsupported or invalid state.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: private_session_does_not_add_listening_knowledge
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - Private Session does not add listening knowledge behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 120
  execution_seconds: 120
  cleanup_seconds: 60
retry_policy:
  mode: CONTROLLED_RETRY
  max_attempts: 2
  reason: Runtime, hardware or live-service behavior may be transient.
  delay_seconds: 5
artifacts:
  - type: logs
    required: true
    description: Sanitized evidence for private session does not add listening knowledge.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Internal, Requires redaction]
destructive: false
tags: [music_dna, music_dna]
estimated_duration: 5m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
