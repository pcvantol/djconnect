id: LOCALIZATION-001
title: Canonical locale set includes en nl de fr es
description: Verify that DJConnect platform behavior for canonical locale set includes en nl de fr es matches the canonical Scenario Catalog expectation.
purpose: Protect the accepted DJConnect platform contract for canonical locale set includes en nl de fr es.
owner: DJConnect Verification
category: Localization
subcategory: localization
priority: P0
risk: high
verification_level: V0
automation_level: FULL
required_components: [HA, Apple, Windows Native ARM64, Pi, ESP32, Website, Release Repo]
supported_platforms: [Home Assistant, iOS, Windows ARM64, Raspberry Pi, ESP32, Website, Release Repo]
required_locales: [all]
required_build_types: [Debug, Instrumented]
required_capabilities: []
required_profiles: []
required_backends: []
required_devices: []
required_environment: [configured_ha_instance]
preconditions:
  - The verification environment contains the components required for canonical locale set includes en nl de fr es.
  - Scenario fixture data is isolated from real user data or explicitly approved for test use.
setup:
  - Prepare the canonical fixture state for canonical locale set includes en nl de fr es.
  - Record environment metadata before execution.
steps:
  - number: 1
    action: Execute the platform flow for canonical locale set includes en nl de fr es.
  - number: 2
    action: Observe backend, client, hardware or artifact behavior as applicable.
  - number: 3
    action: Collect sanitized evidence and compare it to expected results.
assertions:
  localization:
    - User-facing copy follows the canonical five-language contract where applicable.
  privacy:
    - Evidence excludes OAuth tokens, bearer tokens, raw prompts, raw audio, Ask DJ history dumps and Music DNA contents.
expected_results:
  machine:
    - code: canonical_locale_set_includes_en_nl_de_fr_es
      expected: true
    - code: privacy_redaction_complete
      expected: true
  human:
    - Canonical locale set includes en nl de fr es behaves according to the canonical platform baseline without moving ownership into the wrong component.
cleanup:
  - Remove scenario-created state or restore the isolated fixture baseline.
  - Retain only sanitized evidence needed for reporting.
timeouts:
  setup_seconds: 120
  execution_seconds: 60
  cleanup_seconds: 60
retry_policy:
  mode: NONE
  max_attempts: 1
artifacts:
  - type: artifact_metadata
    required: true
    description: Sanitized evidence for canonical locale set includes en nl de fr es.
    redaction: Remove secrets, raw prompts, raw audio, private history and Music DNA contents unless explicitly required and approved.
  - type: environment_snapshot
    required: true
    description: Versions, selected profiles, capabilities and relevant fixture metadata.
privacy_classification: [Public]
destructive: false
tags: [localization, localization]
estimated_duration: 2m
manual_actions: []
future_notes:
  - Phase 4 may bind this scenario to one or more concrete adapters without changing the expected platform behavior.
version: 1
schema_version: "1.0"
