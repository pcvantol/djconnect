# DJConnect repository synchronization

The generated projection is authoritative for generic repository
synchronization, branch/worktree safety and fail-closed preflight. This local
page remains only as a stable navigation target for DJConnect records and does
not restate that generic contract.

For current DJConnect lifecycle records and the historical Engineering Platform
boundary, use `docs/ai-development/DJCONNECT_DEVELOPMENT_EXTENSION.md`.
