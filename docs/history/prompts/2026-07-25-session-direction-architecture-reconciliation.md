# Prompt History: Session Direction Architecture Reconciliation

The Planner already owns current musical direction, while existing Broadcast
projections deliberately exclude Planner state and Flow records only immutable
decisions. A bounded future Session Direction Projection is therefore recorded
as a renderer-safe technical design, not an implementation or new DJMoment path.
